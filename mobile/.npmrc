# pnpm + React Native / Expo: Metro can't resolve symlinked deps reliably, so we
# use a hoisted node_modules layout (flat, like npm/yarn classic).
node-linker=hoisted

# Allow build scripts for native packages (pnpm 10 blocks them by default).
enable-pre-post-scripts=true
